% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cycling_classification.R
\name{plot_cycling_safety_map}
\alias{plot_cycling_safety_map}
\alias{plot_cycling_safety_map.default}
\title{Plot a cycling safety map with summary statistics}
\usage{
plot_cycling_safety_map(classification, show_stats = TRUE, basemap = FALSE)

plot_cycling_safety_map.default(
  classification,
  show_stats = TRUE,
  basemap = FALSE,
  ...
)
}
\arguments{
\item{classification}{A \code{cycling_classification} instance returned by \code{classify_bike_infrastructure()}.}

\item{show_stats}{Logical. If \code{TRUE} (default), prints summary statistics.}

\item{basemap}{Logical. If \code{TRUE}, adds a light basemap tile beneath the
network (requires \code{ggspatial} and internet access). Defaults to \code{FALSE}.}

\item{...}{Further arguments passed to methods.}
}
\value{
A combined \code{patchwork} / \code{ggplot} object, invisibly.
}
\description{
Creates a combined figure with a \code{ggplot2} safety map (left panel) coloured
by infrastructure type and a horizontal bar chart of total length per type
(right panel). Compared to the plain \code{plot()} method for
\code{cycling_classification}, this function always includes the statistics panel
and optionally prints the raw summary table to the console.
}
\examples{
\dontrun{
cl <- classify_bike_infrastructure(get_cycling_network("Muenster, Germany"))
plot_cycling_safety_map(cl)
plot_cycling_safety_map(cl, show_stats = FALSE, basemap = TRUE)
}


}
