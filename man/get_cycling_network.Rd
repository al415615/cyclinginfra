% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cycling_network.R
\name{get_cycling_network}
\alias{get_cycling_network}
\title{Download the cycling network of a city from OpenStreetMap}
\usage{
get_cycling_network(city, crs = 4326, bbox_km = 5)
}
\arguments{
\item{city}{A single non-empty character string passed to
\code{\link[osmdata:getbb]{osmdata::getbb()}}, for instance, \code{"Muenster, Germany"}.}

\item{crs}{A single numeric EPSG code for the output CRS. Defaults to
\code{4326} (WGS 84).}

\item{bbox_km}{Radius (in km) of the square bounding box around the city
centre. Defaults to \code{5}.}
}
\value{
An instance of class \code{cycling_network} (a list with components \code{city}, \code{lines}, and \code{download_date}).
}
\description{
Queries the Overpass API via \code{osmdata} for cycling related way features
within a square bounding box centred on the requested city. Results are saved in cache to disk as \code{.rds} files so repeated calls do not download data all over again.
}
\examples{
\dontrun{
net <- get_cycling_network("Muenster, Germany")
print(net)
plot(net)
}

}
