% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cycling_classification.R
\name{plot.cycling_classification}
\alias{plot.cycling_classification}
\title{Plot a cycling_classification object}
\usage{
\method{plot}{cycling_classification}(x, basemap = FALSE, ...)
}
\arguments{
\item{x}{A \code{cycling_classification} instance.}

\item{basemap}{Logical. Whether to add a basemap underneath the
network (requires \code{ggspatial} and Internet access). Default is \code{FALSE}
so that the plot always works without a network connection.}

\item{...}{Further arguments (currently ignored).}
}
\value{
A \code{ggplot} object, invisibly.
}
\description{
Produces a \code{ggplot2} safety map of the cycling network coloured by
infrastructure type, with an optional base map tile.
}
\examples{
\dontrun{
cl <- classify_bike_infrastructure(get_cycling_network("Muenster, Germany"))
plot(cl)
plot(cl, basemap = TRUE)
}

}
