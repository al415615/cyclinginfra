% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_bike_infrastructure.R
\name{classify_bike_infrastructure}
\alias{classify_bike_infrastructure}
\title{Classify cycling segments by infrastructure type}
\usage{
classify_bike_infrastructure(network)
}
\arguments{
\item{network}{A \code{cycling_network} instance returned by
\code{get_cycling_network()}.}
}
\value{
A \code{cycling_classification} instance.
}
\description{
Takes a \code{cycling_network} instance and assigns each road segment to
one of the five ordered safety levels based on its OpenStreetMap tags. Returns a
\code{cycling_classification} instance that extends the input with the
classification and aggregate length statistics.
}
\details{
\strong{Safety levels} (best → worst):
\describe{
\item{dedicated track}{Physically separated from motor traffic
(\code{highway=cycleway}, \code{cycleway=track}).}
\item{footway track}{Shared path with pedestrians, still separated from
cars (\code{bicycle=designated}).}
\item{painted lane}{Bike lane painted on the road
(\code{cycleway=lane}, \code{cycleway=opposite_lane}, \code{cycleway=opposite}).}
\item{shared lane}{Lane shared with cars, light marking only
(\code{cycleway=shared_lane}).}
\item{shared road}{Bikes permitted on normal road, no dedicated space
(\code{bicycle=yes}).}
}
}
\examples{
\dontrun{
net <- get_cycling_network("Muenster, Germany")
cl  <- classify_bike_infrastructure(net)
print(cl)
summary(cl)
plot(cl)
}

}
